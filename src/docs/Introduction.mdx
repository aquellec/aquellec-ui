import { Meta } from '@storybook/addon-docs/blocks';
import {
  Families,
  Governance,
  Hero,
  IntroHeading,
  IntroText,
  Pillars,
  SectionHeader,
  SharedUtilities,
} from './_showcase';

<Meta title="Docs/Introduction" />

{/*
  This page holds structure only. Every string comes from the story dictionary
  through the blocks below, so the toolbar locale switch drives the page too.
*/}

<Hero />

<SectionHeader section="pillars" id="design-system-pillars" />

<Pillars />

<SectionHeader section="catalog" id="library-architecture" />

<IntroText id="catalogIntro" />

<Families />

<IntroHeading id="composablePatterns" />

<IntroText id="compoundIntro" />

```tsx
<Card>
  <Card.Header title="ATS recommendations" />
  <Card.Body>{/* content */}</Card.Body>
  <Card.Footer>{/* actions */}</Card.Footer>
</Card>

<Modal isOpen={open} onClose={close} title="ATS report" footer={<Button>Close</Button>}>
  {/* content */}
</Modal>
```

<SharedUtilities />

<SectionHeader section="quickStart" id="quick-start" />

<IntroHeading id="install" />

```bash
pnpm add @aquellec/ui lucide-react
# ou : npm install @aquellec/ui lucide-react
```

<IntroText id="peerDependencies" />

<IntroHeading id="configureTailwind" />

<IntroText id="presetIntro" />

```js
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
  presets: [require('@aquellec/ui/tailwind-preset').default],
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
    './node_modules/@aquellec/ui/dist/**/*.{js,mjs}',
  ],
};
```

<IntroText id="esmEquivalent" />

```ts
// tailwind.config.ts
import type { Config } from 'tailwindcss';
import aquellecPreset from '@aquellec/ui/tailwind-preset';

export default {
  presets: [aquellecPreset],
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
} satisfies Config;
```

<IntroHeading id="importComponents" />

```tsx
import { Button, Card, Dropzone, ToastProvider, useToast } from '@aquellec/ui';

export function App() {
  return (
    <ToastProvider>
      <Card className="max-w-md">
        <Card.Header title="Upload a resume" />
        <Card.Body>
          <Dropzone accept=".pdf" maxSizeMB={5} onFileSelect={(file) => console.log(file.name)} />
        </Card.Body>
      </Card>
    </ToastProvider>
  );
}
```

<IntroHeading id="developLocally" />

```bash
pnpm install
pnpm dev              # Storybook -> http://localhost:6006
pnpm build            # ESM/CJS build plus types
pnpm test:storybook   # Vitest interaction and a11y tests
pnpm build-storybook  # Static documentation export
```

<SectionHeader section="governance" id="quality-and-governance" />

<Governance />

<IntroText id="tokensNote" />
