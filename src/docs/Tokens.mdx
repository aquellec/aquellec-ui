import { Meta } from '@storybook/addon-docs/blocks';
import { SectionHeader } from './_showcase';
import {
  ColorScale,
  TokensHeading,
  TokensText,
  ElevationScale,
  FocusRings,
  NeutralText,
  RadiusScale,
  SemanticTokens,
  TokensHero,
  TypeScale,
  Viewports,
} from './_tokens';

<Meta title="Foundations/Tokens" />

{/*
  Structure only. Values come from `src/lib/design-tokens.ts` and copy from the
  story dictionary, so the page follows both the preset and the toolbar locale.
*/}

<TokensHero />

<SectionHeader section="brandColors" id="brand-colors" />

<TokensText id="brandIntro" />

<ColorScale name="brand" />

<ColorScale name="ai" />

<SectionHeader section="semanticTokens" id="semantic-tokens" />

<TokensText id="semanticIntro" />

<SemanticTokens />

<TokensText id="semanticNote" />

<NeutralText />

<SectionHeader section="radii" id="radii-and-elevations" />

<RadiusScale />

<ElevationScale />

<SectionHeader section="typography" id="typography" />

<TypeScale />

<SectionHeader section="focus" id="focus" />

<FocusRings />

<TokensText id="focusNote" />

<SectionHeader section="viewports" id="viewports" />

<Viewports />

<TokensHeading id="pinViewport" />

```tsx
export const OnMobile: Story = {
  render: () => <MyPage />,
  globals: {
    viewport: { value: 'mobile', isRotated: false },
  },
};
```

<TokensText id="pinViewportNote" />

<TokensText id="shortcuts" />

<SectionHeader section="tokenUsage" id="using-the-tokens" />

<TokensHeading id="presetHeading" />

<TokensText id="presetIntro" />

```ts
// tailwind.config.ts
import aquellecPreset from '@aquellec/ui/tailwind-preset';

export default {
  presets: [aquellecPreset],
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
    './node_modules/@aquellec/ui/dist/**/*.{js,mjs}',
  ],
};
```

<TokensText id="presetNote" />

<TokensHeading id="outsideHeading" />

<TokensText id="outsideIntro" />

```ts
import { aquellecColors, aquellecThemeExtensions } from '@aquellec/ui';

aquellecColors.brand[600];              // '#0052cc'
aquellecThemeExtensions.boxShadow.card; // Card surface shadow
```

<TokensText id="outsideNote" />
